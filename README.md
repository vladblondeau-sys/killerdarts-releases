# killerdarts-releases

Binaries only, no source. Holds the installers and latest.yml that the Darts auto-updater polls.
Published by `npm run release`; mirrored to the legacy feed by scripts/dual-publish.mjs for machines on v1.0.103 or older.
